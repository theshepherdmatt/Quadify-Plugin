# ── Quadify Python runtime requirements ──
# Target: Volumio 4 (Bookworm) · Python 3.11 · armhf/arm64
# Install: pip install --break-system-packages -r requirements.txt
# Note: RPi.GPIO comes from apt (python3-rpi.gpio), not pip.

# OLED display — versions are sensitive, keep exact
luma.core==2.4.2
luma.oled==3.13.0
Pillow==9.5.0

# Hardware I/O
smbus2==0.5.0
spidev==3.6

# State machine + signals
transitions==0.9.2
blinker==1.6.3

# Config
PyYAML==6.0.1

# Volumio Socket.IO client
# socketio 4.x and engineio 3.x MUST stay paired — do not bump independently
python-socketio==4.6.1
python-engineio==3.14.2
websocket-client==1.6.1
requests>=2.31.0
# Direct import in library_manager.py (from urllib3.util.retry import Retry);
# pin so a requests bump can't shift the urllib3 major out from under us.
urllib3>=1.26,<3

# SVG → PNG icon rendering (convert2.py / IconProvider)
CairoSVG>=2.7
cairocffi>=1.6
tinycss2>=1.2
cssselect2>=0.7
defusedxml>=0.7

